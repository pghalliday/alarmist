import createReducer, {
  taskStart,
  taskLog,
  taskEnd,
  logSelector,
  headerSelector,
} from '../../../../../../../src/cli/monitor/ui/types/task/reducer';
import {
  createStore,
} from 'redux';
